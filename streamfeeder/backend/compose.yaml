version: "3"
services:
  database:
    image: "docker.io/library/postgres:18-alpine"
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: "postgres"
      POSTGRES_PASSWORD: "postgres"
      POSTGRES_DB: "streamfeeder"
    volumes:
      - ./test-db-data:/var/lib/postgresql/data
